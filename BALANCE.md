# 밸런스 · 패치 로그

> 원칙: 플레이어에게만 적용되는 규칙은 상대 AI에게도 자동으로 동일하게 적용한다.
> (시야/안개, 날씨 이동 페널티, 폭염 피해 등은 양 진영 대칭)

이 문서는 그리드 전술 RPG(weapon-kind grid tactics) 엔진 기준의 밸런스·표현 계층 변경 이력을 기록한다.

---

## 스토리 캠페인 2막 《영웅의 깃발》 추가 (R5~8)

- **2막(R5~8)** 데이터 추가: R5 무너진 성문(청류성 공성) / R6 포로의 길 / R7 피로 세운 다리 / R8 서쪽의 사냥개(카일 첫 등장). 맵 4종(16×14/15×12/18×13/17×14) 추가.
- **동료 확장**: 설화(R5 임시→R6 정식)·백린(R7 지원→R8 정식) 실제 등장. `joinBefore`는 그 라운드 신규 합류 동료만 강제 출전(기존 동료는 병영에서 자유 편성).
- 카일 로젠하임은 R8 적 지휘관으로 첫 등장(정식 합류는 후속 막). 스토리 폴백 경계 R9로 이동.

## 스토리 캠페인 1막 《재가 된 깃발》 + 컷씬·동료·레벨 난이도 (신규)

- **스토리 모드 신설**(`src/game/campaign/story/`): 새 게임이 스크립트 캠페인(`Campaign.mode==='story'`)으로 시작. 전 20라운드 기획 중 **1막(R1~4)** 구현. 각 라운드 = 전투 전 컷씬 → 전투 → 전투 후 컷씬 → 동료 이벤트. R5+는 절차 전투로 폴백.
- **컷씬 시스템**(`CutsceneScreen.tsx`): 대사창 + 직업 스프라이트 초상(정면), 화자/지문/주인공 주입, 클릭 진행·건너뛰기. `App`에 `story-pre`/`story-post` 흐름 삽입.
- **스토리 동료**: 도윤(R1 임시→R2 정식)·연비(R3 임시→R4 정식). 합류 시 주인공 레벨로 생성해 로스터 편입(`ensureCompanions`/`applyStoryEvents`, 임시/정식 상태 추적). 설화·백린·카일은 데이터만 선반영(후속 막).
- **주인공 레벨 기반 난이도**(`story/difficulty.ts`): 적 기준 레벨 = 주인공 레벨을 라운드 권장 밴드로 클램프. 지휘관은 정예 + `killCommander` 대상.
- **라운드별 맵 주입**: `GridBattleScreen`에 `map`/`spawnsA`/`spawnsB` optional props 추가(미지정 시 기존 기본 맵). 스토리 맵은 라운드별 크기·지형(10×8/14×10/12×10/15×11).
- **엔진 소폭 확장**: `generateCharacter`에 `spriteJob` 지정 옵션(명명 동료·적 초상), `enemyParty`의 `BEHAVIOR_FOR_KIND` export.
- **툴링**: `.claude/`(이식 플러그인)를 knip/oxlint 스캔에서 제외(`knip.json`·`.oxlintrc.json`). 시각 검증은 Node 정적서버 + Chromium 헤드리스 스크린샷으로 컷씬·스토리 전투 화면 실렌더 확인.
- 목표 매핑: 시나리오 주 목표를 엔진 지원 3종으로 매핑, 호위·구출 등 미지원 항목은 선택 목표 문구로 안내(엔진 강제는 후속).

## 2단계 P2h — 초반 시스템 해금(§44) + 특성·옵션 추가 배선

- **점진 해금**(`campaign/unlocks.ts`): 동료 모집 2R·상점 3R·장비 강화 4R. 파티 편성·인벤토리는 항상 가능. 미해금 탭은 잠금(🔒N R) 비활성, 강화 버튼은 잠금 배지로 대체. 순수 함수(`recruitFromCandidate`·`buyShopItem`·`enhanceEquip`)도 해금 전에는 no-op. 승리로 새 시스템이 열리면 결과 화면에 "🔓 새 시스템 해금" 안내.
- **특성 추가 배선**: 경량 보행(lightStep) — 장비 총무게가 적재량 절반 이하면 이동력 +1(최대 5, `effectiveMove`). `WIRED_TRAIT_IDS`에 추가.
- **장비 옵션 추가**(§31): 회피(evasion) 옵션 신설 — 희귀/전설 장비에 등장, `evasionChance`에 가산(`equippedOptionTotal`). 옵션 종류 3 → 4종.
- `npm run verify`(test 186)+build 통과. 해금·강화·특성·옵션 테스트 추가, 기존 모집/상점 테스트를 해금 라운드에 맞춰 갱신.

---

## 2단계 P2g — 적 AI 4종(§39)

- **AI 행동 유형 4종**(`engine/ai.ts`, `Character.aiBehavior`): 돌격(aggressive)·견제(skirmisher)·수비(defensive)·지원(support). 기존 단일 추격 AI 위에 유형별 이동 성향을 얹었다.
  - 견제: 사거리를 유지하며 카이팅(공격 가능 타일 중 적과 최대 거리). 근접 무기면 돌격으로 대체.
  - 수비: 위협 반경(맨해튼 4) 밖이면 아군 무게중심 근처 대기, 접근 시 교전.
  - 지원: 체력 70% 미만 아군 우선 치료, 없으면 수비형처럼 대기.
- **유형 배정**(`BEHAVIOR_FOR_KIND`): 검·창·단검=돌격, 둔기·방패=수비, 궁·석궁·투척·법장=견제, 주술서=지원. 적 생성 시 무기 종류로 결정.
- **표시**: 전투 정보 카드에 적 AI 유형 라벨. 도발·시야·마지막 목격 위치 등 기존 규칙과 병존.
- `npm run verify`(test 178)+build 통과, AI 유형 테스트 6종 추가.

---

## 2단계 P2f — 전투 불능·부상·묘지(§42)

- **부상 시스템**(`campaign/casualties.ts`, `Character.injured`): 전투 불능(HP 0) → 부상. 부상 캐릭터는 전투 시작 HP ×0.7(`INJURED_HP_MULT`, `maxHp` 반영). 부상 상태에서 재차 전투 불능 시 **전사**(로스터 제거·묘지 기록). 출전해 생존한 부상자는 회복, 미출전 부상자는 부상 유지. 승패 무관 적용.
- **주인공 예외**: 주인공은 전사하지 않고 부상 상태를 유지(런 유지).
- **부상 치료**(`treatInjury`): 정비 화면에서 골드로 즉시 해제. 비용 = `20 + 레벨×8`.
- **묘지**(`Campaign.graveyard`): 전사한 동료의 이름·레벨·전사 라운드 기록. 파티 편성 탭 하단에 표시.
- **UI**: 결과 화면 사상자(부상/전사) 목록, 파티 편성 탭 부상 배지·치료 버튼·묘지 목록. `BattleOutcome.downedAllyIds`로 전투 불능 id 전달.
- `npm run verify`(test 172)+build 통과, 사상자 처리 테스트 추가.

---

## 2단계 P2e — 전투 목표(§40) + 전투 평가(§41)

- **라운드별 전투 목표**(`campaign/objectives.ts`): 기본 목표 1개 + 선택 목표 1개(결정적). 보스 라운드=지휘관 처치, `round%4===3`=제한 턴 생존(5라운드), 그 외=적 전멸. 선택 목표는 빠른 승리(제한 라운드 이내) 또는 적 절반 처치.
- **엔진 승리 조건 확장**(`engine/battle.ts` `BattleObjective`): 지휘관 처치 시 잔당이 남아도 즉시 승리(§40), 제한 턴 생존 시 목표 턴 경과·아군 생존이면 승리. 기존 전멸 조건은 기본값.
- **전투 평가 3등급**(§41): 승리 / 완전 승리(선택 목표 달성) / 압도적 승리(선택 목표 + 아군 무손실). 등급별 추가 보상 — 완전 명성 +10·골드 +30, 압도적 명성 +25·골드 +60(`ratingReward`, `settleBattle` 반영).
- **UI**: 파티 편성에 다음 전투 목표 미리보기, 전투 화면 상단 목표 라벨, 결과 화면 평가 등급 표기(⭐).
- `npm run verify`(test 165)+build 통과, 목표·평가 테스트 추가.

---

## 2단계 P2c — 장비 강화(§32)

- **강화 시스템**(`data/enhance.ts`): 실패·파괴 없음, 골드+재료 소모, 최대 5단계(`enhanceLevel`). 강화 총량 = 다음 레벨 단계 무강화 수준(선형 가산). `effectiveWeaponPower`·`armorDefense`에 강화 인자 추가.
- **재료 자원**(`campaign.materials`): 승리 +1·보스 처치 추가 +1. 정비 화면 상단에 표기.
- **무기 강화 부가**: 숙련 경험치 획득 +3%/단계(최대 +15%, `weaponEnhanceExpMult`). 성장 특성 연계 — 수리공(+10% 증가분)·절약가(-15% 골드).
- **UI**: 인벤토리 탭 장비별 강화 버튼(비용·재료 표기), 강화 단계 배지, 무기 공격력 표시에 강화 반영.
- `npm run verify`(test 162)+build 통과, 강화 테스트 추가.

---

## 2단계 P2b — 장비 등급·옵션(§31)

- **3등급 도입**(`data/equipGrade.ts`): 기존 희귀 boolean → **일반/희귀/전설**. 상점 등급 확률(전설 5%·희귀 22%·일반), 가격 배수(×1 / ×1.8 / ×3.2). 인스턴스·상점 상품에 `grade`·`options` 필드 추가(`WeaponInstance`/`ArmorInstance`/`ShopItem`).
- **추가 옵션(희귀 1·전설 1 더 강함)**: 최대 체력 +·무게 감소·정신 저항 +. 전투 반영 — maxHp(`derivedStats`)·장비 무게(`equipment`)·정신 저항(`derivedStats`). 그 외 §31 예시 옵션(지형 명중·상태 지속 감소·기술 횟수)은 후속 배선.
- **전설 파티 유일**(§31): 같은 전설 장비(templateId)는 파티에서 하나만 장착 가능 — 보관함 장착 시 차단(`partyHasLegendaryEquipped`, 무기/방패/방어구).
- **표시**: 상점 카드에 등급 태그·옵션 라벨, 무기 표시명에 `[희귀]/[전설]` 접두.
- `npm run verify`(test 158)+build 통과, 등급/옵션 테스트 추가.

---

## 2단계 P2a — 고유 특성 시스템 + 주인공 생성 플로우

기획안 2단계 착수: 고유 특성(§43) 데이터·배정·표시·핵심 효과 배선과 주인공 생성/특성 선택(§2.1~2.6).

- **고유 특성 66종**(`data/traits.ts`): 7범주(공격/방어/기동/지원/지휘/성장/범용) 데이터 모델. `Character.traitId` 신설. 동료·적은 무기 주 역할 가중치(§43.11)로 무작위 배정.
- **효과 배선(24종, `engine/traitEffects.ts`)**: 상시형(강인한 체질 최대체력 +10%·균형 감각 최저 능력치 +5·방패 숙련·불길 내성·짐꾼·훈련광 숙련경험 +15%·노련한 용병 하한 +5%·수영/설원/숲지기/야간 시야)·회복(구호병/의무병/전술 치료)·조건부 공격(추격자·측면·고립·압박·침착함·질풍·약점 분석)·피해 감소(후방 수비·결속·진형 강화). 나머지는 표시용(효과 후속 배선). 데미지 계산에 `defenderDamageMult`·특성 위력/명중 보정 훅 추가.
- **주인공 생성(§2.1~2.6, `HeroCreateScreen`)**: 주인공은 **레벨 1** 시작(전직 없음·숙련 초보·레벨 1 일반 장비). 이름·성별·무기·방어구(천/가죽) 선택 후 **특성 후보 3개**(공격·방어/기동·지원·지휘/성장·범용) 중 하나 선택·**1회 무료 재추첨**. **튜토리얼(1라운드) 종료 후 1회** 특성 재확인(유지/미선택 후보/새 후보 3개).
- **표시**: 정보 카드·모집 카드에 특성명·효과·"효과 반영" 배지. `newCampaign`이 `HeroSetup`을 받도록 변경.
- `npm run verify`(test 155)+build 통과.

---

## 전투 공식 재조정 R5b — 지형·날씨 가중 이동 비용

- **가중 이동 도입**(기획안 §20·§23): 균일 비용 BFS → **타일 진입 비용 다익스트라**(`computeReachableTiles`). 평지·언덕·숲 1, **물 2**, 눈 날씨는 **평지·숲 +1**. 이동력(예산)에서 물/날씨 감소를 빼던 방식을 폐지하고 진입 비용으로 일원화(`effectiveMove`는 이제 지형·날씨 무관, 상태이상 감소만 반영).
- **언덕 통과 가능화**: 언덕은 더 이상 경로를 막지 않는 **통과 가능한 고지대**. 물만 "진입 가능·통과 불가".
- **최소 1칸 보장**: 예산이 부족해도 인접한 가장 싼 진입 가능 타일 하나로는 이동 가능(`moveStepsForRound`의 정수 하한 규칙 대체·제거).
- **급류 재정의**: rawMove +1 → **물 진입 비용 −1(최소 1)**. **적응력**은 물·눈 추가 비용을 무시.
- **날씨 이동 규칙 정리**: 비의 방어구 이동 -0.5·눈의 방어구 이동 -0.5(`weatherMoveModifier`) 폐지. 비는 시야 -1만, 눈은 진입 비용 +1로 대체.
- 대칭 적용(플레이어·AI). `npm run verify`(test 150)+build 통과, 이동·날씨 테스트 재작성.

---

## 전투 공식 재조정 R5 — 전용기술 수치·동작 조정 + 속도 동률 정렬

- **전용기술 수치**(기획안 §26): 돌진 180/3회 → **170/2회**·방패는 무력화 대신 **방어력 절반(50% 확률·2턴, 보스 1턴)**. 낙석 200 → **180**·**직선 첫 적만 공격 후 뒤로 밀어냄(보스 면역)**. 관통사격 뒤 대상 0.5 → **0.6배**(급소·부가효과 없음). 치료 지력/5 → **마법공격력 50%**. 약화 3회 → **2회(명중 90)**. 원소폭풍 3회 → **2회**. 기습 5회 → **4회**. 저격 3회 → **2회**. 분신 3회 → **2회**. 빠른교체 3회 → **2회**.
- **마법부여 상한**: 결합 주스탯(높은치+낮은치 50%)이 **높은 능력치의 1.5배**를 넘지 못하도록 명시(증폭·마법부여 연계 상한).
- **시야 증가 상한**(기획안 §21): 천리안·언덕·매의눈의 **시야 증가 합계 최대 +2**(`effectiveSight`).
- **광역보호 피해 감소**: 광역보호 상태로 대신 받는 피해 **20% 감소**(`calculateDamage`의 피해 감소 단계).
- **속도 동률 정렬 결정화**(기획안 §3): 기존 rng 동률 처리 → **①라운드 우선권 진영(홀수=플레이어/짝수=적) → ②배치 순서**로 결정적 처리(`determineTurnOrder`).
- 대칭 적용(플레이어·AI). `npm run verify`(test 150)+build 통과.

---

## 전투 공식 재조정 R4 — 속성 상성 개편 + 적 등급(정예/보스) + 상태이상 정비

- **속성 상성 비대칭 그래프**(기획안 §24, `elements.ts`): 기존 5원소 단일 순환에서 **불→나무→물→불** 순환 + 추가 상성 **강철→불·땅→강철·나무→땅**으로 교체. 한 속성이 여러 속성에 강할 수 있다(예: 나무는 물·땅에 강함). 유리 1.3배(증폭 1.6)·불리 0.7배·그 외 1배 유지.
- **적 등급 도입**: `isElite` 신설(+기존 `isBoss`). 정예는 능력치 강화(체력 ×1.5·공격 ×1.1), 4라운드 이후 선두·보스 라운드 친위대로 등장. 등급별 규칙 분기.
- **고정 피해 상한**(기획안 §10): 최대체력 비례 고정피해 상한을 **일반 25 / 정예 15 / 보스 10%**로 통일. 치명사격은 **위력 50%·2회 → 25%(상한 적용)·1회**로 개편(기존 절반-감소 규칙 대체).
- **충격(둔기) 개편**(기획안 §28): 기존 "2턴간 매 턴 30% 행동불가" 제거. 20% 발동 시 **일반 적 다음 행동 취소 / 정예 이동력 −2 / 보스 −1**(`shocked`·`moveDown` 상태 신설).
- **관통(창) 완화**: 방어력 전부 무시 → **50%만 무시**(철갑사격 등 기술 자체 무시율과 큰 쪽 적용).
- **출혈·맹독 상한**(기획안 §29): 두 지속피해가 함께 걸리면 **한 턴 합계 최대체력 20% 상한**. 보스 출혈은 최대체력 비례 대신 시전자 공격력 기반 제한 피해.
- **보스 내성 표**(기획안 §30): 완전 면역이 아닌 약화 전환 — 봉쇄→이동력 −2, 방패 무력화 1턴, 속성 변경 1턴, 밀쳐내기 면역(막히면 추가 피해 20%). 밀쳐내기의 기존 "막히면 50% 기절"은 추가 피해 20%로 대체.
- **UI**: 상태 칩에 충격/둔화 추가(기절 제거), 유닛 툴팁에 [보스]/[정예] 표기.
- 대칭 적용(플레이어·AI). `npm run verify`(test 149)+build 통과, 속성·상태·등급 관련 테스트 갱신·추가.

---

## 전투 공식 재조정 R3 — 전직 3단계 + 무기 숙련도 분리

- **전직 6단계 → 3단계 간소화**(기획안 §10): 초급/중급/고급 전용기술이 각각 **1/2/3차 전직**에서 해금(이전 2/4/6차). **2차** 전직이면 그 무기로 교체 시 턴 소모 없음(이전 3차/티어3), **3차** 전직에서 무기별 전직 패시브 습득(이전 5차). `MAX_TIER 6→3`, 전직 포인트 지급 간격 `5→10`레벨(`PROMOTION_LEVEL_INTERVAL`). 티어1 위력/명중 보너스(`TIER1_BONUS`)는 폐지.
- **무기 숙련도 분리 도입**(기획안 §11): 전직과 **별개**의 4단계 숙련도(초보/숙련/전문/달인)를 신설(`weaponProficiency`). 단계에 따라 **공격 피해 최소 난수 하한**이 오른다 — 초보 0.7 / 숙련 0.8 / 전문 0.9 / 달인 1.0(`proficiencyFloor`). 이전에는 전직 티어가 이 하한을 겸했다.
- **숙련 경험치 획득**: 해당 무기로 **적에게 직접 피해**를 줄 때만 누적하며, 보조기술 반복으로는 오르지 않는다. **한 번의 기술 사용에서 얻는 경험치는 최대 2배**까지(범위 공격 다중 명중 상한, `PROFICIENCY_MAX_GAIN_PER_SKILL`). 생성 캐릭터(적·동료)는 레벨에 맞춘 기본 숙련을 부여하고, 테스트 로스터는 달인으로 고정.
- **UI**: 정보 카드에 장착 무기의 **숙련도 단계** 표기 추가. 모집 카드의 패시브 안내는 "(티어5)"→"(3차)".
- 대칭 적용(플레이어·AI). `npm run verify`(test 146)+build 통과, 전직/숙련 관련 테스트 갱신.

---

## 전투 공식 재조정 R2 — 명중·회피 통합 판정

- **명중·회피 단일화**(기획안 §8): 기존에는 ①캐스팅 시점의 스킬 명중 판정 + ②대상별 회피 판정으로 **두 번** 굴렸다. 이제 피해를 주는 공격은 **대상별로 한 번만** 판정한다 — `최종 명중률 = 기술 명중률 + 지형·상태 보정 − 대상 회피율`, **[20, 100] 클램프**(`hitChanceAgainst`). 위력 0 보조·디버프 기술만 캐스팅 시점에 명중 판정(회피 미적용, 정신력 저항은 별도 유지).
- **지형·상태 보정**: 현재 값이 있는 항목은 티어1 숙련 명중 보너스(둔기 +5·활 +10)뿐이며, 그 외 지형·상태 명중 보정은 0(향후 확장 지점). 활의 **집중** 발동 시 회피율 항을 무시(기존 규칙 유지).
- **범위 공격 명중 개별화**: 원소폭풍·낙석 등 다중 대상 공격도 이제 대상마다 개별 명중 판정을 거친다(이전엔 캐스팅 1회 판정으로 전원 일괄 적중/불발).
- 대칭 적용(플레이어·AI). `npm run verify`(test 146)+build 통과, 통합 명중 회귀 테스트 2종 추가.

---

## 전투 공식 재조정 R1 — 파생 능력치·핵심 공식

기획안(47개 항) 반영의 1단계: 기존 메커니즘에 대한 전투 공식·규칙 정비(신규 메타 시스템은 후속 단계로 분리).

- **최대 체력 파생화**: 이전에는 `hp` 능력치가 곧 최대 체력이었으나, 이제 **최대 체력 = `20 + 체력 × 3 + 레벨 × 2`**(`maxHp`). 체력 능력치와 레벨이 함께 체력 풀을 결정한다. 지속피해(출혈·맹독 1/8, 화염 1/4, 폭염 1/16)·치료 상한·고정피해(치명사격) 등 체력 비율 계산은 전부 `maxHp` 기준으로 전환. HP 바·정보 카드·모집/편성 카드·유닛 툴팁도 최대 체력 표기로 통일.
- **회피율 개편**: `(스피드/305)/2 + 레벨차/100` → **`스피드/1000`(스피드 1당 0.1%p), 상한 30%, 레벨차 미반영**(`evasionChance`). 저스피드 캐릭터의 과도한 회피와 레벨 격차로 인한 회피 폭증을 제거.
- **이동 하한 정정**: 이동력이 1 미만이면 "홀수 라운드에만 1칸" 규칙을 제거하고 **항상 최소 1칸 이동** 가능(`moveStepsForRound = max(1, floor(이동력))`).
- **데미지 공식 상·하한**: 조건부 위력배수·속성배율·급소를 합친 **피해배수를 최대 2.5배로 제한**하고, 방어를 무시한 기술 피해의 **10%를 최소 데미지로 보장**(고방어 상대에게도 최소 타격 확보, 극단적 배수 곱연산 방지). 최소값 1 floor 유지.
- **무기 부가효과 확률 세분화**: 일괄 30% → **검 출혈 30·둔기 충격(기절) 20·창 관통 30·활 집중 30·석궁 급소 20**(`PROC_CHANCE`). 마법서·투척은 인스턴스가 선택한 효과의 확률을 따른다.
- 대칭 원칙에 따라 위 모든 변경은 플레이어·AI 양쪽에 동일 적용. `npm run verify`(typecheck·test 144·lint·knip)+build 통과, 관련 테스트를 새 공식에 맞춰 갱신.

---

## 캠페인 4단계 — 파티 편성 심화 + 후반 스케일

- **파티 편성 심화**(`PartyFormationTab`): 출전/대기 분리, **출전 순서 = 배치 위치**(↑↓ 재정렬 → 스폰 슬롯 순서로 반영, `App`이 `deployedIds` 순서로 팀 구성), 각 출전 유닛의 무기·방어구·**전용기술(로드아웃)** 확인.
- **후반 스케일**: `enemyStatMult`(8라운드 이후 라운드당 +4%)로 후반 적 능력치 강화, `generateCharacter`에 `statMult` 옵션 추가. 보스 처치 결과 화면에 "👑 보스 처치!" 표시 + 파티 편성에 보스 보상 안내.
- 순수 로직 vitest 3종(스케일 배수·statMult 반영·후반 적 강화). 이로써 캠페인 4단계(코어 루프·모집·상점·편성) 완료.

---

## 캠페인 3단계 — 상점 + 보관함(구매·판매·장착)

- **상점**(`rollShop`): 라운드마다 무기·방어구·방패(희귀 포함) 6종 갱신, 가격 = 레벨×8×(희귀 2.2), 기준 레벨은 라운드에 따라 상승. 승리로 라운드 진행 시 모집 후보와 함께 상점도 갱신.
- **보관함(stash)**(`campaign/stash.ts`): 구매·해제 장비를 보관. 인벤토리 탭에서 캐릭터에 **장착(현재 장비와 교체·착용 레벨 검증, 양손 무기 시 방패 자동 해제)** 또는 **판매**(레벨×4). 로드아웃은 무기 교체 시 사용 가능 스킬로 자동 정리.
- `ShopTab` + `InventoryScreen` 보관함 패널, `App` 핸들러(구매·장착·판매) 연결. 순수 로직 vitest 6종(상점 생성·가격·구매 차감/부족·판매·장착 교체·착용 레벨 미달).

---

## 캠페인 2단계 — 동료 모집

- **동료 모집** 구현: 명성 품질 티어(0/20/50/100 경계 → 일반병사/숙련병/엘리트/영웅급)로 후보 3~5명 생성(`rollRecruits`), 매 라운드(승리로 진행 시) 갱신되어 지나간 후보는 사라진다. 최대 30명.
- 후보 카드에 직업·레벨·능력치·무기(인스턴스명)·방어구·고유 패시브(무기 티어5)·비용 표시. **골드로 모집**(비용 = 레벨×15×품질 배수: 일반1·숙련1.4·엘리트1.9·영웅2.6). 골드 부족·인원 초과 시 버튼 비활성.
- `promotions.ts`에 `weaponPassiveLabel` 추가(패시브 한글명), 정비 모집 탭(`RecruitTab`) 연결. 순수 로직 vitest 5종(품질 경계·후보 생성/고유id·비용·모집 차감/부족).

---

## 캠페인 1단계 — 코어 루프·명성·골드·적 생성·보스

- 앱을 **라운드형 캠페인**으로 재편(기존 2팀 샌드박스는 "빠른 전투"로 잔류). 타이틀→직업 선택→정비→전투→결과 정산→정비 루프, **localStorage 저장**(새 게임/이어하기).
- **명성**: 기본 `(라운드-1)×10` + 성과(적 전멸 +20·아군 생존 ×5·보스 처치 +50). **골드**: 처치 +10·클리어 +라운드×20·보스 +100.
- **캐릭터 생성기**(`generateCharacter`): 무기·레벨 기반 능력치(합 `25+(L-1)×3`)·장비·전직·로드아웃 자동. **적 파티 생성기**(`generateEnemyParty`): 라운드 테마(창/궁/마법/중장/암살/속성)·인원·레벨 스케일, 5라운드마다 보스.
- **보스 도입**(`isBoss`): 이전 "보스 미도입"을 뒤집어 활성화 — 고정 피해(치명사격) 절반, 기절·봉쇄·약화·방패 무력화 저항. 체력·공격 강화.
- `createCharacter`에 `weaponMastery`·`skillLoadout` 활용, 정비 인벤토리 탭은 `InventoryScreen`을 캠페인 로스터로 재사용(판매/stash는 다음 단계).
- 참고: 이번 단계는 코어 루프·파티 편성·인벤토리 조회까지. **동료 모집·상점·판매/장착 이동**은 다음 단계. 순수 로직 vitest 12종 추가(명성·골드·생성·적 파티·정산). Playwright 미설치 → 화면 전환은 build+로직·헤드리스 루프로 검증.

---

## 창 개편 — 사거리 1 확정 + 전직 패시브 반격

- **창 사거리 2 → 1**로 정정(스펙 기준). 전용기술(꿰뚫기·봉쇄·돌진)은 변경 없음.
- 창 전직 패시브 **전열유지 → 반격(counter)**: 창 사거리 내 적에게 **직접 공격으로 피해를 입은 경우** 공격자에게 **기본 공격 위력의 0.5배**로 반격(라운드당 1회). 데미지 훅의 전열유지(인접 아군 ×1.2)는 제거.
  - 제외: 범위 공격에 휘말린 경우, 공격자가 창 사거리 밖인 경우, 경호 등으로 다른 아군이 대신 피격한 경우. 반격은 전용기술 효과·협공·분신·기타 추가 공격을 발동시키지 않음(재귀 차단).

---

## 전투 표시 — 피격 대상 위 데미지 숫자·빗나감

- 공격 시 피격 대상 위에 **입힌 데미지 숫자**를 띄운다(급소는 크게·붉은색+`!`). **회피/명중 실패 시 "빗나감"**, 치료 시 **+회복량**(초록)도 표시.
- 엔진이 턴마다 `GridBattle.lastTurnEvents`(대상별 데미지/빗나감/회복)를 누적하고, UI가 대상별로 합산해 유닛 위에 잠깐 떠오르는 플로팅 텍스트로 렌더(다단 히트·범위 피해는 합산).

---

## 테스트 로스터 개편 — 동양풍 인명·무기별 1인·레벨30 6차전직

- 로스터 10명을 **직업명 → 동양풍 인명**으로 변경(강도현·백건우·서지훈·윤재하·한도영·류시아·오세준·임하늘·문서준·정유안).
- 각 캐릭터가 **서로 다른 무기 1종**을 장착(검·둔기·창·활·석궁·단검·투척·불 지팡이·물 지팡이·마법서 — 9개 무기 종류 전부 커버, 지팡이는 불/물 2종).
- 전원 **레벨 30**, 장착 무기를 **6차 전직**(티어6, 초/중/고급 전용기술 전부 사용 가능). 기본 로드아웃도 해당 무기 전용기술 3종 + 기본 공격.
- 능력치·장비를 레벨/무기에 맞게 조정: 5개 능력치 합 112(레벨30 분배 포인트 87), 무기 주스탯 위주 배분, 무기·방어구·방패 Lv.30(적재량 내), `createCharacter`에 `weaponMastery`·`skillLoadout` 옵션 추가.

---

## 전투 시스템 전면 개편 — 무기별 전용기술 27종·전직 패시브 9종·공통 규칙

- **전용 기술 전면 재정의**: 9개 무기(검·둔기·창·활·석궁·마법서·지팡이·단검·투척) 모두 **초(T2)/중(T4)/고급(T6) 3개씩** 구현. 기존 검·둔기·활·지팡이·마법서 스킬(발도·각성·고속연타·단결·분쇄·화공·급소·원기회복 등)은 새 스킬로 교체, 창·석궁·단검·투척은 신규.
  - 검: 반월참(부채꼴)·일섬(넉백)·섬광참(돌진). 둔기: 다리타격(이동-1)·밀쳐내기(넉백/기절)·광역보호(경호 강화). 창: 꿰뚫기(관통 2타)·봉쇄(이동불가)·돌진(방패 무력화). 활: 천궁(언덕 사거리+1)·도약사격(추가이동)·저격(5칸/차단). 석궁: 철갑사격(방어 20% 무시)·관통사격(직선 2타)·치명사격(최대체력 50% 고정피해). 마법서: 치료·정화(디버프 제거)·재행동. 지팡이: 원소탄·약화·원소폭풍(속성별 이름 자동변환, 광역 중심150/주변100). 단검: 기습(추가이동)·은신(투명)·축지(아군 인접 순간이동). 투척: 맹독(지속피해, 출혈 중복)·분신(0.3배 추가타)·쇄상(디버프 대상 ×1.3).
- **공통 기술 개정**: 보호·급류·등반·천리안·투시는 지속 중 재사용 불가, 마법부여 주스탯이 **max(근,지)+0.5×min(근,지)**로 변경, **빠른교체** 신규(3턴 무기 교체 무료·턴당 1회·전용기술 잠금).
- **전직 티어 의미 정리**: 1차 숙련도, 2차 초급, 3차 무기교체 무료, 4차 중급, 5차 패시브, 6차 고급. **티어5 패시브 9종 신설**: 질주(검)·경호(둔기)·전열유지(창)·매의눈(활)·정조준(석궁)·명상(마법서)·증폭(지팡이)·적응력(단검)·협공(투척). (기존 저체력/방어/시야/속성방벽/사용횟수 패시브는 대체)
- **신규 상태이상**: 광역보호·봉쇄·은신·분신·맹독·빠른교체. 제거: 각성·단결·급소(스킬 상태).
- **반응·위치이동 시스템**: 경호/협공/분신 반응 공격(라운드당 1회, 추가공격의 추가공격 재발동 차단), 도약사격·기습 공격 후 추가이동(플레이어 수동 선택), 축지 순간이동 후 추가행동, 은신 투명(시야 제외·이동 차단·공격/피격 시 해제).
- **공통 전투 규칙**: 고정피해는 방어/속성/급소/위력증감 무시, 0% 보조기술 위력증가 미적용, 추가공격 재귀 차단, 동명 강화/약화 미중첩(지속시간 갱신), 1회 기술은 보존/회복 대상 아님. **보스 개념 미도입**(보스 감소 조항 생략).
- UI: 상태칩 신규 상태 라벨·설명 추가, 지팡이 원소 스킬 이름 자동변환 표시, 축지 목적지·후속 이동 칸 하이라이트.
- 참고: Playwright 미설치 → UI(후속 이동·축지 하이라이트, 상태칩)는 typecheck·vitest(113개)·build로 검증. 시각 확인은 사용자 확인 필요.

---

## 양손 파지 무기공격력 1.3배

- 단검·투척무기를 제외한 모든 무기는 방패(보조 무기) 없이 두 손으로 들 수 있고, **양손 파지 시 무기공격력 ×1.3**(`effectiveWeaponPower`). 데미지 공식의 무기공격력에 반영(플레이어·AI 대칭).
  - 양손 전용 무기(대검·창·활·석궁·지팡이)는 항상 적용, 한손 무기(환도·철퇴·주술서·마법서)는 방패를 빼면 적용, 단검·투척은 미적용.
- 인벤토리 창·전투 장비교체 창에 각 무기의 공격력 수치와 "양손 ×1.3(적용)" 배지를 표시.

---

## 상태 설명 · 조회 팝업 전환 · 무기 속성/부가효과 표시

- **상태 정보 설명**: 하단 바 버프/디버프 칩을 누르면 해당 상태의 뜻을 설명하는 한 줄이 아래에 표시(다시 누르면 닫힘). 14종 상태이상 전부에 설명 문구(`STATUS_DESC`) 부여, 마우스오버 툴팁으로도 노출.
- **조회 팝업 동작**: 캐릭터 정보 카드가 떠 있을 때 **다른 캐릭터를 누르면 그 캐릭터로 자동 전환**, **빈 타일을 누르면 팝업이 닫힘**. 조회 대상은 아군 전체 + 시야 안의 적군.
- **무기 인스턴스 표시 이름**(`weaponInstanceName`): 지팡이는 속성을 붙여("불의 법장"), 마법서·투척무기는 부가효과를 붙여("출혈의 주술서", "기절의 투척무기") 표시. 인벤토리·장비교체 창·정보 카드·하단바에 일괄 적용.
- 참고: 마법서/투척 인스턴스별 부가효과, 지팡이 인스턴스별 속성(+초급/고급·주술 기술이 지팡이 속성 상속), 속성 상성(불<물<나무<강철<땅<불; 약점 ×1.3·강점 ×0.7·무속성 무영향) **로직은 이미 엔진에 구현되어 있었고**, 이번 변경은 이를 UI에 노출·표시하는 표현 계층 작업이다.

---

## 날씨 이동 페널티를 방어구 기준으로 수정(버그)

- 비/눈 이동 페널티가 **무기 종류**로 판정되던 버그 수정 → **착용 방어구**로 판정. 비=중갑·판금(-0.5), 눈=천·가죽·미착용(-0.5). 이제 판금→가죽으로 갈아입으면 비 페널티가 사라진다.

---

## 기술 타입 · 장비교체 창 · 이동 불가 안내

- **기술 타입(물리/마법/변화)** 분류(`skillTypeLabel`) 추가 및 표시(인벤토리 목록 + 전투 버튼 배지). 주술·지팡이·마법서 전용=마법, 그 외 위력 있음=물리, 위력 없음=변화. (데미지 주스탯은 물리=근력·마법=지력으로 기존 damageType과 일치)
- **장비교체 창**: 하단 "장비교체"가 순환 선택에서 **모달 창**으로 변경. 보유 무기/방어구를 보여주고(착용 레벨 미달은 잠금) 하나 선택 후 완료 시 교체+턴 종료.
- **이동 불가 안내**: 이동력 1 미만이라 이번 턴 이동할 수 없을 때 하단 바에 "이동력이 1 미만이라 이번 턴에는 이동할 수 없습니다 (2턴에 1칸)" 안내 표시.

---

## 레벨업 포인트 분배제 · 데미지/이동 공식 · 무기 부가효과 도입

- **스탯 체계 전면 개편**: 기존 `defense`(방어) 스탯을 제거하고 `endurance`(지구력)를 새 5번째 점수-분배 스탯으로 추가. 점수로 찍을 수 있는 5개 능력치(체력/근력/지력/스피드/지구력)는 전원 초기값 5, 레벨업마다 3포인트를 인벤토리 화면에서 직접 분배(`unspentStatPoints`/`spendStatPoint`, 기존 자동 스탯 성장 제거). 최대 레벨 100 상한 적용.
- **방어력이 완전히 파생 스탯화**: 더 이상 기본 스탯이 아니며 방어구+방패 수치만으로 결정. 데미지 공식도 `최종공격력 − 방어력`(뺄셈) 방식으로 교체(기존 나눗셈식 폐기).
- **데미지 공식 교체**: `최종공격력 = (주스탯/6 + 무기공격력) × 숙련도~100% 랜덤값 × 기술위력`. 숙련도 랜덤값 하한은 전직 티어별 placeholder(티어0=50%~티어6=100%). 방패 방어 보너스 수치를 새 공식 규모에 맞춰 15/25 → 3/6으로 재조정.
- **정신력·회피율 신설**: 정신력 `(지력/305)×0.7`(상대 디버프·부가효과 무시 확률, 최대 70%), 회피율 `(스피드/305)/2 + 레벨차/100`(공격 회피 확률, [0,1] 클램프). 회피 판정은 명중 판정 통과 후 방어측이 한 번 더 굴리며, 활의 '집중' 발동 시 생략.
- **이동력 공식 교체**: `(지구력/30)+1`, 최대 5칸. 5 초과분은 이동 감소 페널티를 먼저 상쇄하는 버퍼로만 작용(END가 높을수록 슬로우 저항력↑). 이동력 1 미만이면 2턴에 1칸(홀수 라운드에만).
- **무기 부가효과 신설**(적중 시 30% 확률 발동): 검=출혈(2턴 DoT 1/8), 둔기=기절(2턴간 매 턴 30% 별도 판정), 창=관통(방어력 전부 무시), 활=집중(회피율 무시), 석궁=급소(×1.5), 방패=없음. 마법서·투척무기는 인스턴스마다 위 5개 중 하나를 선택해 지님. 출혈·기절 적용은 정신력 저항 판정을 거침(기존 도발/다리부상/약화 디버프에도 동일 저항 규칙을 소급 적용).
- **단검 이도류 지원**: 주무기가 단검일 때만 보조 슬롯(방패 슬롯 재사용)에 단검을 하나 더 장착 가능. 다른 무기와는 병용 불가, 보너스 효과 없음(순수 슬롯 허용).
- 로스터 10명의 스탯을 새 5스탯 모델(레벨10 기준 5×5+27=52 포인트)로 재설계.

---

## 버그 수정 — 시야 밖일 때 AI가 멈춰있던 문제

- **증상**: 교전 전이나 적이 시야를 벗어난 상태에서는 `pickAiAction`이 빈 행동(`{}`)을 반환해 AI가 제자리에서 계속 대기했다. 두 팀이 상하로 멀리 떨어져 시작하는 맵 구조상(기본 시야 5 vs 스폰 거리 약 9) 첫 교전까지 양쪽 다 서로를 못 보는 경우가 흔해, 사실상 아무도 움직이지 않는 것처럼 보였다.
- **수정**: `GridBattle`이 각 진영이 상대 유닛을 마지막으로 확인한 위치를 `knownEnemyPositions`에 기억(시야를 벗어나도 유지)하도록 하고, AI는 시야 안에 실제 대상이 없으면 그 기억 중 가장 가까운 위치로, 그마저 없으면 맵 중앙으로 계속 이동하도록 변경. 공격은 여전히 실제로 시야에 들어오거나 도발로 강제된 대상에게만 시도(추측 위치로 공격하지 않음 — 플레이어와 동일한 정보 제약 유지).

---

## 장비(무기 · 방어구) 시스템 전면 개편

- **적재량 스탯 신설**: 기본 5kg + 근력 5당 1kg. 장착·소지 중인 모든 무기·방어구 무게 합이 적재량을 넘을 수 없다.
- **무기 공격력을 레벨 기반 공식으로 통일**: 기존 무기 종류별 고정 `basePower`를 제거하고, 모든 무기 인스턴스가 10단위 착용 레벨(10~100)을 가지며 공격력 = `착용 레벨 / 2`(단검은 3/4배)로 계산한다.
- **방어구(3번째 장비 슬롯) 신설**: 천/가죽/중갑/판금 4종. 방어력 = 같은 레벨 무기의 기준공격력 × 종류별 배수(0.7/0.9/1.1/1.3). 무게는 0.5/1/3/5kg. 기존 방패(shield)와는 별개의 슬롯이며 방패 로직은 그대로 유지.
- **착용 레벨 제한**: 캐릭터 레벨이 장비 레벨 이상이어야 장착 가능. 인벤토리에서 미달 항목은 버튼 비활성화 + 필요 레벨 표시.
- **인벤토리 슬롯 제한**: 기본 장착 장비 외 최대 2개까지만 추가 소지 가능(적재량 범위 내).
- **전투 중 장비 교체 통합**: 기존 무기교체 버튼이 무기와 방어구를 하나의 순환 목록으로 함께 보여주도록 확장(레벨 미달 항목은 후보에서 제외). 방어구 교체는 항상 턴을 소모.
- **로스터 시작 레벨 1 → 10**: 전원 Lv.10 무기/방어구로 시작. 창/석궁/단검/투척무기 종류의 기본 무기 템플릿도 추가(전용 스킬은 아직 미구현, 공통 스킬만 사용 가능).
- 데이터 무결성 회귀 테스트(`roster.test.ts`) 추가: 전 로스터 캐릭터의 소지 장비 무게가 적재량 이내, 추가 소지 장비 ≤2개, 기본 장착 장비가 착용 레벨 조건을 만족함을 검증.

---

## 아군 정보 카드

- 맵에서 아군(현재 차례가 아닌 아군 포함)을 누르면 좌측 상단에 정보 카드 표시: 원화·이름·Lv·무기/방패·HP·능력치·이동/시야·버프/디버프. 플레이어 턴·적 턴 모두 조회 가능, 닫기 버튼 또는 행동 시 닫힘.

---

## 탐사한 지형 기억(스타크래프트식 안개)

- 한 번이라도 시야로 밝힌 타일은 이후 시야를 벗어나도 **지형 색을 어둡게(brightness 0.4)** 기억해 표시. 한 번도 못 본 타일만 균일한 암흑(지형 미상). 전투별로 탐사 타일을 누적 기록(현재 유닛 위치는 탐사 구역에 표시하지 않음).

---

## 암흑 타일 지형 정보 은닉

- 시야로 밝히지 못한 타일은 지형별 색/아이콘 대신 **균일한 어두운 타일(tile-unknown)**로 표시해 **어떤 지형인지 알 수 없게** 함(이전엔 어둡게 덮여도 지형 색이 희미하게 비쳤음).

---

## 시야 밖 숲 타일 암흑 처리

- 기존에는 숲 타일이 시야와 무관하게 항상 표시됐으나, 이제 **시야 밖 숲 타일도 다른 타일처럼 암흑(안개)**으로 덮는다. (숲 내부 유닛 은폐 로직은 그대로)

---

## 기획 문서화 · 자동 갱신 규칙

- 게임 기획·상세 사양 정본 문서 `DESIGN.md` 신설(현재까지의 모든 시스템/타일/전투/UI 정리).
- 작업 지침 `CLAUDE.md` 신설: 앞으로 **모든 변경마다 사용자가 말하지 않아도 `DESIGN.md`와 `BALANCE.md`를 함께 갱신**하도록 표준화. README에서 세 문서 링크.

---

## 적군 체력 빨간색 · 버프/디버프 표시

- 적군(B팀) 체력바는 잔량과 무관하게 항상 빨간색으로 표시(아군과 구분).
- 보호(가드) 반경은 요청대로 마름모 1칸(직교 인접 4칸) 유지. 직교 인접 아군 보호는 정상 동작(테스트로 확인). ※ 대각선은 마름모 거리 2라 미보호. ※ 보호는 시전자 자신이 아니라 "주변 아군 대신 피격"이므로 시전자 직격은 그대로 피해.
- 하단 행동 바 우측 상단에 현재 캐릭터의 버프/디버프와 남은 턴 수 칩 표시(버프=녹색, 디버프=적색).

---

## 타일 정보 정비(스펙 반영)

- **바위(rock, 신규)**: 장애물(진입 불가). 원거리·마법 공격이 바위를 넘어 타격 불가(직선 경로에 바위가 있으면 차단). 아이콘 🪨, 맵에 중앙·가장자리 배치.
- **숲(forest)**: 인접 1칸 밖에서는 은폐(기존). 추가로 **숲 안에서는 근접 무기만 행동 가능**(원거리·마법 무기는 스킬 사용 불가).
- **물(water)**: 진입은 되지만 **넘어서 이동 불가**(경로 종착). **물 위 이동력 -1**(급류로 상쇄).
- **언덕(hill)**: 이제 **등반 버프 없이도 올라갈 수 있음**(버그 수정). 넘어서 이동 불가(경로 종착), **언덕 위 시야 +1**, **올라선 턴에는 행동 불가**(등반 버프 시 예외).
- **불(fire)**: 해당 타일 캐릭터 체력 1/4 감소(기존), 숲이 불타면 평지로 변경(점화 시 즉시). 추가로 **불 타일 포함 주변 1칸의 캐릭터는 시야와 무관하게 확인 가능**(양 진영).

(사거리·범위·반경은 모두 마름모(맨해튼) 기준으로 판정.)

---

## 기술 범위·반경도 마름모

- AoE 폭발 범위·자신범위/아군범위 버프·보호 반경 등 모든 반경 계산(`aliveUnitsInRadius`)과 보호 대상 판정을 마름모(맨해튼)로 변경. 이제 사거리·범위·반경·시야·이동이 전부 마름모로 통일.

## 무기 사거리 마름모 · 기술 즉시 발동

- 무기/기술 **사거리 판정을 마름모(맨해튼)**로 변경(대상까지 대각선 1칸=거리 2). 대상 지정형·타일형 사거리, AI 사거리·접근 판정 모두 반영. (AoE 폭발 범위·보호 반경은 기존 유지)
- **기술 즉시 발동**: 타겟이 필요 없는 기술(자신/자신범위/아군범위)은 기술 버튼만 눌러도 즉시 발동. 대상 지정형은 **타겟(적/타일)을 누르면 즉시 발동**(확인 불필요). 대기 중이던 이동이 있으면 이동 후 발동.
- 확인 버튼은 이동 확정/무기교체/대기(턴 종료)용으로만 남김.

## 이동 범위 마름모

- 이동 가능 범위를 시야와 동일하게 **마름모(맨해튼)** 형태로 변경. BFS를 8방향에서 직교 4방향으로 바꿔, 대각선 1칸은 이동력 2를 소모(직교 2칸 경유). 플레이어·AI 공통.

## 기본 시야 통일

- 모든 로스터 캐릭터의 기본 시야를 5로 통일(기존 3/4/5 → 5). 날씨·시간대 보정(밤 -2, 비/눈 -1)은 그대로 적용.

## 시야 마름모(맨해튼 거리)

- 시야 판정을 체비셰프(사각형)에서 **맨해튼 거리(마름모)**로 변경. 대각선 1칸은 시야 2를 소모.
- 이동은 그대로 8방향(체비셰프, 대각선=1). 무기 사거리·범위 판정도 기존(체비셰프) 유지 — 시야만 마름모.

## 안개 렌더링 수정(깨져 보이는 현상)

- 날씨/시간대 틴트를 스크롤되는 뷰포트가 아니라 고정된 무대(`.battle-stage`)에 올려, 카메라가 이동해도 화면 전체를 균일하게 덮도록 수정(밤 틴트가 절반만 덮여 안개가 깨져 보이던 문제).
- 격자 gap 배경을 밝은 회색(#333)에서 어두운 색(#14151d)으로 바꿔, 안개 구역에서 틈이 밝은 '균열'로 보이지 않게 함.
- 시야 수치 자체(밤 -2, 비/눈 -1)는 정상. -3처럼 보이던 것은 위 렌더링 문제로 가장 바깥 시야 링이 어둡게 가려졌기 때문.

## 시간대·날씨 시야 보정

- 밤에는 캐릭터 시야 **-2**, 비 또는 눈일 때 **추가 -1**(중첩 가능). 시야는 최소 1로 보정.
- `effectiveSight`에 시간대·날씨 조건을 반영하고, 안개/타일 공개·적 가시성·타겟팅·AI 시야 판정에 모두 적용(플레이어·AI 대칭).

---

## 하단 행동 바 2배 · 기술 2×2 · 무기교체

- 하단 행동 바 두께를 약 2배로. 기술 버튼을 2×2 그리드로 배치.
- 그 옆(우측) 컨트롤: 위=무기교체 버튼, 아래=취소·확인. ("턴 넘기기" 버튼 제거 — 대기 중 행동이 없으면 확인이 "대기"(턴 종료)로 동작.)
- 무기교체: 클릭 시 인벤토리의 다른 무기(방패 제외)를 순환 선택, 확인 시 교체(엔진 규칙상 숙련 티어<3이면 교체가 턴을 소모).
- 상단 바 — 맵 — 하단 바 flex 구성 유지(하단 바는 flex 흐름상 별도 높이라 맵을 가리지 않음).

## 보드 카메라 시점(스타크래프트식) · 하단 행동 바

- 타일 하나를 정사각형(86px)으로 고정. 보드가 화면(뷰포트)보다 커지고, 카메라가 현재 행동 유닛을 화면 중앙으로 따라가며 스크롤(스크롤바 숨김). 숨은 상대 차례엔 카메라를 옮기지 않음.
- 화면 세로 구성: 상단 바(시간/날씨/행동순서) — 맵(카메라) — 하단 행동 일자바. 맵 높이는 상·하단 바를 제외한 나머지를 전부 차지.
- 행동 컨트롤(기술/취소·확인·턴넘기기)을 좌측 하단 플로팅 패널에서 맵 하단 전체 폭 일자바로 이동. 기술 버튼은 한 줄에서 가로 스크롤.

---

## 공격 모션 · 기본 공격(주먹) · 플로팅 기술 패널

- **공격 모션**: 공격 시 시전자가 상대 방향으로 돌진(아군=위, 적=아래)하고, 피격 대상은 흔들림 + 붉은 섬광. 플레이어·AI 모두 적용.
- **기본 공격 주먹**: `(스킬 없음)` 버튼 제거. 사용 가능한 스킬이 하나도 없으면 자동으로 주먹으로 대체.
  - 주먹: 공통 / 물리 / 위력 50% / 명중 100 / 사거리 1 / 사용제한 없음. 선택 목록·기본 로드아웃에는 노출되지 않는 폴백 전용.
- **플로팅 기술 패널**: 기술/확인 버튼을 맵 좌측 하단 플로팅 패널로 이동(⚔ 버튼으로 접기/펼치기).
- **정사각형 프레임**: 전투 보드를 남은 공간에서 최대 정사각형으로 잡고 가운데 정렬(카메라 스크롤 유지).

---

## 행동 순서 바 · 상태 텍스트 위치

- **상단에 행동 순서 바 추가**: 참여 중인 모든 캐릭터를 스피드(무기 보정 포함) 내림차순으로 나열하고, 현재 차례를 ▶·금색 테두리로 강조.
  - 암흑(플레이어 시야 밖)으로 공개되지 않은 상대는 `???`로 표시(수·행동 순서 위치는 노출되나 정체는 숨김).
- **상태/설명 텍스트 이동**: 현재 유닛 정보와 최신 로그 텍스트를 기술 버튼 바로 위(액션 패널)로 이동.

---

## 이동 미리보기 · 시간대 · 타일/안개 표현

- **이동 미리보기**: 아군이 이동할 칸을 지정해 행동을 고르는 대기 상태에서, 캐릭터 토큰을 예정 칸으로 미리 옮겨 보여준다(카메라도 예정 칸을 추적).
  - 이때 시야 계산은 아직 이동 전 위치 기준이라, 예정 칸 인접 1칸의 숲에 숨은 상대는 **행동을 확정한 이후에** 드러난다.
- **시간대**: 전투 시작 시 아침/낮/저녁/밤 중 무작위 결정. 맵 상단에 시간대·날씨 표시. 밤/저녁/아침에는 보드에 옅은 색 틴트(현재는 정보성/연출 위주).
- **평지 타일 색**: 갈색 → 연두색 계열로 변경.
- **암흑(안개) 표현**: 완전 검정이 아니라 투명도를 준 검정으로 덮어 시야 밖 지형이 희미하게 비치도록 완화.

---

## 스킬 로드아웃 · 화면 구성 · 흐름 개편

- **스킬 로드아웃(최대 4개)**: 전투에 들고 갈 스킬을 캐릭터별로 최대 4개까지 선택.
  - `skillLoadout`을 캐릭터에 추가. `getLoadoutSkillIds`가 사용 가능 스킬 ∩ 로드아웃(최대 4)을 반환하며,
    플레이어 UI와 AI가 모두 이 목록만 사용해 대칭 적용.
  - 미설정 시 사용 가능 스킬의 앞 4개(강타·주술·보호·도발 등)를 기본 사용.
- **타일 아이콘**: 숲🌲 / 언덕⛰️ / 물🌊 / 화염🔥 아이콘 표시(평지는 아이콘 없음). 숲은 실루엣이 항상 보임.
- **상하 배치**: 좌우 대결에서 상하 대결로 변경. 아군(A팀)은 화면 아래, 적(B팀)은 위. 스폰/지형을 가로 방향으로 재배치.
- **이동 가능 칸 강조**: 파란 오버레이 + 펄스 링으로 이동 가능한 칸을 뚜렷하게 표시(공격 대상은 붉은 오버레이).
- **흐름 개편**: 홈에서 캐릭터 선택 후 바로 전투 시작. 능력치·기술·무기 세팅은 별도 "인벤토리" 버튼에서 진행.
  - 인벤토리는 로스터 인스턴스를 직접 편집하고, 전투 시작 시 고유 id로 복제해 사용(양 팀 중복 선택 대비).

---

## 표현 계층 복원 + 날씨/지형 재이식 (element-grid-tactics 재작성 이후)

`feature/element-grid-tactics` 재작성으로 기술/전직/속성 시스템은 새 아키텍처로 재구현되었으나,
이전 버전에서 반영했던 표현 계층과 날씨 시스템이 사라져 아래를 다시 반영했다.

### 표현 계층

- **캐릭터별 원화 스프라이트 복원**: 전투 유닛이 단일 플레이스홀더(SVG)로 덮이던 것을 되돌리고,
  직업·성별별 PNG 원화(`assets/jobs/{spriteJob}-{gender}-{front|back}.png`)로 표시.
  - 각 로스터 캐릭터에 `spriteJob`/`gender` 매핑 추가.
  - 아군(A팀)은 후면(back), 상대(B팀)는 정면(front) 방향으로 표시.
  - 스프라이트를 찾지 못하면 아무것도 렌더하지 않아 캐릭터가 사각형 이미지로 덮이지 않는다.
- **카메라 추적 복원**: 전투 보드를 고정 픽셀(64px) 셀로 만들고 스크롤 뷰포트에 담아,
  현재 행동 유닛을 화면 중앙으로 부드럽게 추적. 단, 플레이어에게 보이지 않는(안개/숲 은폐) 상대의
  차례에는 카메라를 옮기지 않고 아군 시점을 유지한다.
- **전장의 안개 / 숲 은폐 복원**:
  - 플레이어(A팀) 시야 밖 타일은 안개로 덮는다. 숲 타일은 실루엣이 항상 보이되 내부 유닛은 은폐.
  - 플레이어 시야에 들어오지 않은 상대 유닛 토큰은 표시하지 않는다.
  - 시야 밖 상대는 타겟팅(공격 대상 지정)도 불가.

### 날씨 (맑음 / 비 / 눈 / 폭염)

- 전투 시작 시 무작위로 하나 결정(양 진영 공통).
- **비**: 중장(근접: 검·둔기·창·방패) 계열 `rawMove` −0.5.
- **눈**: 경장(원거리·마법: 활·석궁·단검·투척·지팡이·마법서) 계열 `rawMove` −0.5.
- **폭염**: 턴 시작 시 최대 체력 1/16 피해. 단, 정신력(마법공격력 기반, 최대 70%) 확률로 저항.
  - 저항 확률 = min(0.7, 마법공격력 / 40).
- **맑음**: 페널티 없음.

이동 페널티는 `effectiveMove`에, 폭염 피해는 턴 시작 처리에 통합되어 플레이어·AI 모두에 대칭 적용된다.
